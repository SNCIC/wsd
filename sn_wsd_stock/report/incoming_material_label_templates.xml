<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    Incoming material label ZPL shell: the label-center engine produces the
    full ^XA..^XZ payload for every lot (see report/incoming_material_label.py);
    this qweb-text template only echoes it verbatim.
    -->
    <template id="report_incoming_material_label_zpl">
        <t t-translation="off"><t t-out="zpl"/></t>
    </template>
</odoo>
