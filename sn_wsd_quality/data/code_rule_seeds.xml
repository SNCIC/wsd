<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Seed coding rules for the quality family (mes-coding-rule batch 4).
         The seeder reads the legacy ir.sequence current values at load
         time, so it must load after data/ir_sequence.xml. -->
    <function model="sn.code.rule.seed.quality" name="seed" eval="[]"/>
</odoo>
