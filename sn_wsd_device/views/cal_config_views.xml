<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_cal_config_form" model="ir.ui.view">
        <field name="name">sn.wsd.device.cal.config.form</field>
        <field name="model">sn.wsd.device.cal.config</field>
        <field name="arch" type="xml">
            <form string="Calibration Settings">
                <group>
                    <group>
                        <field name="trigger_time" placeholder="08:00"/>
                    </group>
                </group>
                <footer>
                    <button name="action_save" type="object"
                            string="Save" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_cal_config" model="ir.actions.act_window">
        <field name="name">Calibration Settings</field>
        <field name="res_model">sn.wsd.device.cal.config</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
