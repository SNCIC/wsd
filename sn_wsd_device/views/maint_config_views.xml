<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_maint_config_form" model="ir.ui.view">
        <field name="name">sn.wsd.device.maint.config.form</field>
        <field name="model">sn.wsd.device.maint.config</field>
        <field name="arch" type="xml">
            <form string="Maintenance Settings">
                <group>
                    <group>
                        <field name="trigger_time" placeholder="08:30"/>
                    </group>
                </group>
                <footer>
                    <button name="action_save" type="object"
                            string="Save" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_maint_config" model="ir.actions.act_window">
        <field name="name">Maintenance Settings</field>
        <field name="res_model">sn.wsd.device.maint.config</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
