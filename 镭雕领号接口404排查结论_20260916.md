# 镭雕（激光）批量领号接口 404 排查结论

> **档位变更提示（2026-09-22 补注）**：本文是 2026-09-16 15:51–16:00 的现场记录。当时「资源不存在」档位**确实是 404**，
> 故设备端日志、Device API Logs、探针返回均如实记为 404 —— 这些是历史事实，未作改写。
> 同日 16:23 服务端已将资源类不存在统一并入 **422**（提交 `8787857`：404 归还路由层，仅表示路径不存在或服务未部署）。
> 因此阅读正文时：**凡本文写作 404 的业务拒绝点，现今一律返回 422**，下文「二、根因 → 校验顺序」中的 404 档位同样按 422 理解。
> 变的只是状态码数字，中文 message 内容一致；排查顺序、命中原因与设备端缺陷结论**完全不受影响**。

- 接口：`POST http://192.168.127.160/api/v1/laser/print-requests`
- 现象：设备端报 `GetHttpPostData异常: 远程服务器返回错误: (404) 未找到。`，随后 `未将对象引用设置到对象的实例`
- 排查时间：2026-09-16 15:51–16:00（北京时间）
- 结论：**服务端接口正常，是设备端把「组织机构标识」等字段填成了占位值 `1`**；404 是本接口的**业务错误码**，不是路径不存在。
  设备端因未读取响应体，把服务端的中文原因丢掉了，又因空响应判空缺失抛出次生异常，掩盖了真实原因。

## 一、证据链

### 设备端日志（截图）

| 时间         | 内容                                               |
| ------------ | -------------------------------------------------- |
| 15:34:54.831 | `url: http://192.168.127.160/api/v1/laser/print-requests` |
| 15:34:55.000 | `param: {"M_DATA_AUTH":"1","workOrderNo":"1","quantity":1,"panelQty":1,"drawingNo":"1","operator":"1"}` |
| 15:34:55.119 | `GetHttpPostData异常: 远程服务器返回错误: (404) 未找到。` |
| 15:34:55.163 | `GetNewSnCodeUrl: response`（响应内容为空）        |
| 15:34:55.173 | `雕刻机调用mes接口或者雕刻工单信息异常: 未将对象引用设置到对象的实例。` |

### 服务端「设备API日志」同一条记录

Manufacturing → 追溯性 → Device API Logs，`id=14`，`create_date = 2026-09-16 07:34:56 UTC`（= 15:34:56 北京时间，与设备端日志同一笔）：

```json
{
  "endpoint": "/api/v1/laser/print-requests",
  "result_code": "404",
  "result_message": "组织机构 1 不存在。",
  "employee_code": "1",
  "payload": {"M_DATA_AUTH": "1", "workOrderNo": "1", "quantity": 1,
              "panelQty": 1, "drawingNo": "1", "operator": "1"}
}
```

payload 与设备端打印的参数**逐字一致** → 就是这一笔请求。

### 服务端复现（现场实测，均为零写入探针）

| 探针报文                                                        | 服务端返回                                   | 说明                     |
| --------------------------------------------------------------- | -------------------------------------------- | ------------------------ |
| `{}`                                                            | `400 组织机构为空。`                         | 路由存在、Service 可达   |
| `{"M_DATA_AUTH":"1","workOrderNo":"<不存在>"...}`               | `404 组织机构 1 不存在。`                    | **首个 404 点**          |
| `{"M_DATA_AUTH":"zone","workOrderNo":"<不存在>"...}`            | `404 制造订单 <X> 不存在。`                  | `zone` 才是正确组织码    |
| `{"M_DATA_AUTH":"zone","workOrderNo":"WH/MO/00027"...}` 操作员假 | `404 员工 <X> 在 MES 中不存在。`             | 订单号通过，卡在操作员   |
| `{"M_DATA_AUTH":"zone","workOrderNo":"00027"...}`              | `404 制造订单 00027 不存在。`                | 订单号是**全等**匹配     |

另：`GET http://192.168.127.160/` → `303 → /odoo`（`Server: nginx/1.24.0 (Ubuntu)`），`GET :8069` → `303 → /odoo`（Werkzeug）
→ nginx 与 Odoo 均正常，`/api/v1/laser/print-requests` 在 80 口可用，**不需要改端口**。

## 二、根因

### 1. 直接原因：请求字段全是占位值

| 字段        | 设备端发出 | 中南生产库实际要求                                                        |
| ----------- | ---------- | ------------------------------------------------------------------------- |
| M_DATA_AUTH | `1`        | 公司**注册号**，中南仪表有限公司 = **`zone`**                             |
| workOrderNo | `1`        | `mrp.production.name` 全等值，如 `WH/MO/00027`（现有单号 `WH/MO/00020~00027`） |
| operator    | `1`        | 在册员工：`hr.employee.barcode` **或** `user_id.login`                      |
| quantity    | `1`        | 合法                                                                      |
| panelQty    | `1`        | 合法，但语义是「**每个拼版的板数**」，=1 表示每个 SN 单独建一个 1 板拼版   |
| drawingNo   | `1`        | 仅留痕不校验                                                              |

校验顺序（服务端代码 `sn_wsd_api/models/api_scan_pass.py::submit_laser_print_request`）：
组织机构(404) → workOrderNo 非空(400) → 制造订单全等匹配(404) → quantity 1~10000(422) → panelQty ≥0(422) → 操作员在册(404) → **之后才开始写 SN/拼版**。
即：只改组织机构，下一笔仍会在「制造订单」404；订单改了还会在「操作员」404。

### 2. 次生原因：设备端错误处理缺陷（这是现场看不见真实原因的关键）

- .NET `HttpWebRequest.GetResponse()` 对非 2xx 一律抛 `WebException`，异常里**带着服务端 JSON**（`{"code":404,"message":"组织机构 1 不存在。","data":false}`），
  但 `MesInterface.Common.Utils.GetHttpPostData` 只把 `ex.Message`（`(404) 未找到`）记出来，**响应体被丢弃**。
- 上抛后 response 为空，`MesClient.GetNewSnCodeUrl` 未判空就取值/反序列化 → `未将对象引用设置到对象的实例`，
  这个次生异常覆盖了真正的业务原因，于是问题被误判成「接口 404 / 地址不对」。

## 三、待办

### 设备端（MesInterface，需供应商改）

1. `GetHttpPostData` 捕获 `WebException`，读 `ex.Response` 流并返回响应体；记录 HTTP 状态码 + **响应体全文** + 完整请求体。
2. `GetNewSnCodeUrl` 对空响应判空，先解析 `{"code","message"}`，`code != 200` 走错误分支并显示 `message`（中文原因直接上界面/日志）。
3. 联调参数改为真实值：`M_DATA_AUTH=zone`、`workOrderNo=WH/MO/000xx`、`operator=<员工工号或登录名>`；
   不需要拼版就传 `panelQty: 0` 或省略。
4. 建议接入字典接口做下拉/校验，避免手填：`/api/v1/manufacturing-orders/by-work-order`、`/api/v1/work-centers/search`、`/api/v1/defect-codes/search`。

### 服务端（我方）

1. ✅ 已更正 `myaddons/接口文档.md`：`workOrderNo` 由「ilike 匹配」改为「**全等精确匹配**」，并补充排错章节与客户端接入要求。
   其中排错章节已于 2026-09-22 按 422 新契约重写（原稿写作「404 ≠ 路径不存在」，现为「业务性 422 ≠ 报文出错，404 专属路由层」）。
2. 待确认：中南 186 名员工抽样 `barcode` 为空（只有 `user_id.login`，且登录名是中文姓名如「彭武林」）。
   现场若习惯扫工号，需先维护员工工号，否则 `operator` 只能送登录名。
3. 待确认：`panelQty` 文档措辞建议明确为「每个拼版的板数」。

## 四、验收步骤

1. 用正确参数发一笔（会真实建 SN/拼版，请在现场确认后执行）：

```bash
curl -s --noproxy '*' -X POST -H 'Content-Type: application/json' \
  -d '{"M_DATA_AUTH":"zone","workOrderNo":"WH/MO/00027","quantity":8,"panelQty":4,"drawingNo":"DWG-TEST","operator":"<工号或登录名>"}' \
  http://192.168.127.160/api/v1/laser/print-requests
```

预期：`{"code":200,"message":"OK","data":{"productSnList":[...8 个...],"quantity":8,"panelQty":4}}`。

2. 随后在设备API日志确认 `result_code=200`，并在制造订单 `WH/MO/00027` 上看到新生成的 SN 与拼版。
3. 故意发一次错参，验证设备端界面/日志能显示出服务端的中文 `message`（而不是 `(404) 未找到`）。
